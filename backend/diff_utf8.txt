diff --git a/src/controllers/LensPurchaseChallan.controller.js b/src/controllers/LensPurchaseChallan.controller.js
index fa28539..7aa1d16 100644
--- a/src/controllers/LensPurchaseChallan.controller.js
+++ b/src/controllers/LensPurchaseChallan.controller.js
@@ -978,3 +978,35 @@ async function updatePurchaseChallanItemStatus(req, res) {
     res.status(500).json({ success: false, error: err.message });
   }
 }
+
+const patchLensPurchaseChallanDcId = async (req, res) => {
+  try {
+    const { id } = req.params;
+    const { dcId } = req.body;
+    if (!id) return res.status(400).json({ success: false, message: "ID required" });
+    const updated = await LensPurchaseChallan.findByIdAndUpdate(
+      id,
+      { $set: { dcId: dcId || "" } },
+      { new: true }
+    );
+    if (!updated) return res.status(404).json({ success: false, message: "Challan not found" });
+    return res.status(200).json({ success: true, data: updated });
+  } catch (err) {
+    console.error("Error patching challan dcId:", err);
+    return res.status(500).json({ success: false, message: "Failed to update DC ID", error: err.message });
+  }
+};
+
+export {
+  addLensPurchaseChallan,
+  getAllLensPurchaseChallan,
+  getLensPurchaseChallan,
+  removeLensPurchaseChallan,
+  editLensPurchaseChallan,
+  createLensPurchaseInvoice,
+  createChallanFromInvoice,
+  updatePurchaseChallanStatus,
+  updatePurchaseChallanItemStatus,
+  getNextBillNumberForPurchaseChallan,
+  patchLensPurchaseChallanDcId
+};
